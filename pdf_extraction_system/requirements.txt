# PDF Invoice Extraction System - Requirements

# Core PDF processing
pdfplumber>=0.9.0
pypdf>=3.0.0

# OCR support (recommended for scanned documents)
pytesseract>=0.3.10
pdf2image>=1.16.0
Pillow>=10.0.0

# AI-powered extraction (OPTIONAL - for ANY invoice format)
# Enables intelligent extraction with 95%+ accuracy
# Cost: ~$0.02 per invoice
# Get API key from: https://console.anthropic.com/
anthropic>=0.18.0  # Claude API for AI extraction

# Note: For OCR to work, you also need system dependencies:
# - Tesseract-OCR: Download from https://github.com/UB-Mannheim/tesseract/wiki
#   Install to C:\Program Files\Tesseract-OCR
#   During install, select Bulgarian language pack (bul.traineddata)
# 
# - Poppler: Download from https://github.com/oschwartz10612/poppler-windows/releases/
#   Extract to C:\Program Files\poppler
#   Add bin folder to system PATH
#
# If OCR not needed (text-based PDFs only), skip pytesseract, pdf2image, Pillow

# AI Extraction Setup:
# 1. Install: pip install anthropic
# 2. Get API key from: https://console.anthropic.com/
# 3. Set environment variable: export ANTHROPIC_API_KEY="your-key"
# 4. See CLAUDE_AI_SETUP.md for complete guide
